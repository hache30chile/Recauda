@model Recauda.Models.VContribuyente

@{
    ViewData["Title"] = "Eliminar Contribuyente";
}

<h1 class="mt-4">@ViewData["Title"]</h1>
<ol class="breadcrumb mb-4">
    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
    <li class="breadcrumb-item"><a asp-action="Index">Contribuyentes</a></li>
    <li class="breadcrumb-item active">Eliminar</li>
</ol>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <i class="fas fa-exclamation-triangle me-1"></i>
                Confirmar Eliminación de Contribuyente
            </div>
            <div class="card-body">
                <div class="alert alert-danger" role="alert">
                    <h5 class="alert-heading">
                        <i class="fas fa-exclamation-triangle"></i> ¡Atención!
                    </h5>
                    <p>Estás a punto de eliminar permanentemente el siguiente contribuyente del sistema. Esta acción <strong>NO</strong> se puede deshacer.</p>
                    <hr>
                    <p class="mb-0">Por favor, revisa cuidadosamente la información antes de confirmar.</p>
                </div>

                <div class="row">
                    <div class="col-md-2 text-center">
                        <div class="bg-danger rounded-circle d-inline-flex align-items-center justify-content-center"
                             style="width: 60px; height: 60px;">
                            <i class="fas fa-user fa-lg text-white"></i>
                        </div>
                    </div>
                    <div class="col-md-10">
                        <h4 class="mb-3">@Model.per_nombre_completo</h4>

                        <div class="row">
                            <div class="col-md-6">
                                <table class="table table-bordered">
                                    <tr>
                                        <td class="bg-light" width="40%"><strong>ID Contribuyente:</strong></td>
                                        <td>@Model.Id</td>
                                    </tr>
                                    <tr>
                                        <td class="bg-light"><strong>RUT:</strong></td>
                                        <td>@Model.per_rut_completo</td>
                                    </tr>
                                    <tr>
                                        <td class="bg-light"><strong>Nombre:</strong></td>
                                        <td>@Model.per_nombre_completo</td>
                                    </tr>
                                    <tr>
                                        <td class="bg-light"><strong>Estado:</strong></td>
                                        <td>
                                            @if (Model.con_activo)
                                            {
                                                <span class="badge bg-success">Activo</span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-danger">Inactivo</span>
                                            }
                                        </td>
                                    </tr>
                                </table>
                            </div>
                            <div class="col-md-6">
                                <table class="table table-bordered">
                                    <tr>
                                        <td class="bg-light" width="40%"><strong>Compañía:</strong></td>
                                        <td>@Model.com_nombre</td>
                                    </tr>
                                    <tr>
                                        <td class="bg-light"><strong>Recaudador ID:</strong></td>
                                        <td>@Model.rec_id</td>
                                    </tr>
                                    <tr>
                                        <td class="bg-light"><strong>Motivo:</strong></td>
                                        <td>@Model.mdc_nombre</td>
                                    </tr>
                                    <tr>
                                        <td class="bg-light"><strong>Valor Aporte:</strong></td>
                                        <td><strong class="text-success">$@Model.con_valor_aporte.ToString("N0")</strong></td>
                                    </tr>
                                </table>
                            </div>
                        </div>

                        <div class="row mt-3">
                            <div class="col-12">
                                <div class="card bg-light">
                                    <div class="card-body">
                                        <h6><i class="fas fa-calendar"></i> Información de Fechas</h6>
                                        <div class="row">
                                            <div class="col-md-4">
                                                <small class="text-muted">Fecha Inicio:</small><br>
                                                <strong>@Model.con_fecha_inicio.ToString("dd/MM/yyyy")</strong>
                                            </div>
                                            <div class="col-md-4">
                                                <small class="text-muted">Fecha Fin:</small><br>
                                                <strong>
                                                    @if (Model.con_fecha_fin.HasValue)
                                                    {
                                                        @Model.con_fecha_fin.Value.ToString("dd/MM/yyyy")
                                                    }
                                                    else
                                                    {
                                                        <span class="text-muted">Sin fecha fin</span>
                                                    }
                                                </strong>
                                            </div>
                                            <div class="col-md-4">
                                                <small class="text-muted">Periodicidad:</small><br>
                                                <strong>Mensual - Día @Model.con_dia_del_cargo</strong>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="alert alert-warning mt-4" role="alert">
                    <h6 class="alert-heading">
                        <i class="fas fa-info-circle"></i> Consideraciones importantes:
                    </h6>
                    <ul class="mb-0">
                        <li><strong>Historial de cobros:</strong> Se mantendrá el historial existente, pero no se generarán nuevos cobros</li>
                        <li><strong>Datos de la persona:</strong> La información personal permanecerá en el sistema</li>
                        <li><strong>Reportes:</strong> Los reportes históricos incluirán este contribuyente</li>
                        <li><strong>Relaciones:</strong> Se eliminará la relación con recaudadores y compañías</li>
                        <li><strong>Recuperación:</strong> No podrás recuperar esta información una vez confirmada la eliminación</li>
                    </ul>
                </div>

                <form asp-action="Eliminar" method="post" class="mt-4">
                    @Html.AntiForgeryToken()
                    <input asp-for="Id" type="hidden" />
                    <div class="row">
                        <div class="col-12 text-center">
                            <button type="submit" class="btn btn-danger me-3">
                                <i class="fas fa-trash"></i> Sí, Eliminar Definitivamente
                            </button>
                            <a asp-action="Detalles" asp-route-id="@Model.Id" class="btn btn-info me-3">
                                <i class="fas fa-eye"></i> Ver Detalles
                            </a>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-times"></i> Cancelar
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card border-warning">
            <div class="card-header bg-warning text-dark">
                <i class="fas fa-lightbulb me-1"></i>
                Alternativas Recomendadas
            </div>
            <div class="card-body">
                <h6>En lugar de eliminar, considera:</h6>
                <ul>
                    <li><strong>Desactivar:</strong> Cambiar el estado a "Inactivo"</li>
                    <li><strong>Modificar fechas:</strong> Establecer una fecha de fin</li>
                    <li><strong>Cambiar asignación:</strong> Reasignar a otro recaudador</li>
                </ul>

                <div class="mt-3">
                    <a asp-action="Editar" asp-route-id="@Model.Id" class="btn btn-warning btn-sm w-100 mb-2">
                        <i class="fas fa-edit"></i> Editar en su lugar
                    </a>
                </div>

                <hr>

                <h6 class="text-danger">Si eliminas:</h6>
                <ul class="small mb-0">
                    <li>No se generarán más cobros automáticos</li>
                    <li>Los reportes futuros no incluirán este contribuyente</li>
                    <li>Se perderá la configuración de aportes</li>
                </ul>
            </div>
        </div>

        <div class="card mt-3 border-info">
            <div class="card-header bg-info text-white">
                <i class="fas fa-chart-line me-1"></i>
                Impacto de la Eliminación
            </div>
            <div class="card-body">
                <div class="text-center">
                    <h5 class="text-danger">$@Model.con_valor_aporte.ToString("N0")</h5>
                    <small class="text-muted">Se perderá este aporte mensual</small>
                </div>

                <hr>
                <div class="text-center">
                    <small class="text-muted">Impacto anual estimado:</small><br>
                    <strong class="text-danger">-$@((Model.con_valor_aporte * 12).ToString("N0"))</strong>
                </div>
            </div>
        </div>
    </div>
</div>