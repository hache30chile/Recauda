@model Recauda.Models.MotivoDeCobro

@{
    ViewData["Title"] = "Eliminar Motivo de cobro";
}

<h1 class="mt-4">@ViewData["Title"]</h1>
<ol class="breadcrumb mb-4">
    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
    <li class="breadcrumb-item"><a asp-action="Index">Motivos de cobro</a></li>
    <li class="breadcrumb-item active">Eliminar</li>
</ol>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <i class="fas fa-exclamation-triangle me-1"></i>
                Confirmar Eliminación del Motivo de Cobro
            </div>
            <div class="card-body">
                <div class="alert alert-danger" role="alert">
                    <h5 class="alert-heading">
                        <i class="fas fa-exclamation-triangle"></i> ¡Atención!
                    </h5>
                    <p>Estás a punto de eliminar permanentemente el siguiente motivo de cobro del sistema. Esta acción <strong>NO</strong> se puede deshacer.</p>
                    <hr>
                    <p class="mb-0">Por favor, revisa cuidadosamente la información antes de confirmar.</p>
                </div>

                <div class="row">
                    <div class="col-md-2 text-center">
                        <div class="bg-danger rounded-circle d-inline-flex align-items-center justify-content-center"
                             style="width: 60px; height: 60px;">
                            <i class="fas fa-user-tag fa-lg text-white"></i>
                        </div>
                    </div>
                    <div class="col-md-10">
                        <h4 class="mb-3">@Model.mdc_nombre</h4>

                        <table class="table table-bordered">
                            <tr>
                                <td class="bg-light" width="30%"><strong>ID:</strong></td>
                                <td>@Model.Id</td>
                            </tr>
                            <tr>
                                <td class="bg-light"><strong>Nombre:</strong></td>
                                <td>@Model.mdc_nombre</td>
                            </tr>
                        </table>
                    </div>
                </div>

                <div class="alert alert-warning mt-4" role="alert">
                    <h6 class="alert-heading">
                        <i class="fas fa-info-circle"></i> Consideraciones importantes:
                    </h6>
                    <ul class="mb-0">
                        <li>Si hay información asignada a este motivo de cobro, perderán la asociación</li>
                        <li>Esta acción eliminará permanentemente toda la información de la compañía</li>
                        <li>No podrás recuperar esta información una vez confirmada la eliminación</li>
                        <li>Se recomienda revisar si hay dependencias antes de eliminar</li>
                    </ul>
                </div>

                <form asp-action="Eliminar" method="post" class="mt-4">
                    @Html.AntiForgeryToken()
                    <input asp-for="Id" type="hidden" />
                    <div class="row">
                        <div class="col-12 text-center">
                            <button type="submit" class="btn btn-danger me-3">
                                <i class="fas fa-trash"></i> Sí, Eliminar Definitivamente
                            </button>
                            <a asp-action="Detalles" asp-route-id="@Model.Id" class="btn btn-info me-3">
                                <i class="fas fa-eye"></i> Ver Detalles
                            </a>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-times"></i> Cancelar
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card border-warning">
            <div class="card-header bg-warning text-dark">
                <i class="fas fa-lightbulb me-1"></i>
                Alternativas Recomendadas
            </div>
            <div class="card-body">
                <h6>En lugar de eliminar, considera:</h6>
                <ul>
                    <li><strong>Renombrar :</strong> Si sólo necesitas cambiar el nombre</li>
                </ul>

                <div class="mt-3">
                    <a asp-action="Editar" asp-route-id="@Model.Id" class="btn btn-warning btn-sm w-100">
                        <i class="fas fa-edit"></i> Editar en su lugar
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>