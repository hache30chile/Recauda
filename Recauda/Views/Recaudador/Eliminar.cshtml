@model Recauda.Models.VRecaudador

@{
    ViewData["Title"] = "Eliminar Recaudador";
}

<h1 class="mt-4">@ViewData["Title"]</h1>
<ol class="breadcrumb mb-4">
    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
    <li class="breadcrumb-item"><a asp-action="Index">Recaudadores</a></li>
    <li class="breadcrumb-item active">Eliminar</li>
</ol>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <i class="fas fa-exclamation-triangle me-1"></i>
                Confirmar Eliminación de Recaudador
            </div>
            <div class="card-body">
                <div class="alert alert-danger" role="alert">
                    <h5 class="alert-heading">
                        <i class="fas fa-exclamation-triangle"></i> ¡Atención!
                    </h5>
                    <p>Estás a punto de eliminar permanentemente el siguiente recaudador del sistema. Esta acción <strong>NO</strong> se puede deshacer.</p>
                    <hr>
                    <p class="mb-0">Por favor, revisa cuidadosamente la información antes de confirmar.</p>
                </div>

                <div class="row">
                    <div class="col-md-2 text-center">
                        <div class="bg-danger rounded-circle d-inline-flex align-items-center justify-content-center"
                             style="width: 60px; height: 60px;">
                            <i class="fas fa-users-cog fa-lg text-white"></i>
                        </div>
                    </div>
                    <div class="col-md-10">
                        <h4 class="mb-3">@Model.Nombre</h4>

                        <table class="table table-bordered">
                            <tr>
                                <td class="bg-light" width="30%"><strong>ID:</strong></td>
                                <td>@Model.Id</td>
                            </tr>
                            <tr>
                                <td class="bg-light"><strong>Usuario:</strong></td>
                                <td>@Model.Nombre</td>
                            </tr>
                            <tr>
                                <td class="bg-light"><strong>Compañía:</strong></td>
                                <td>@(Model.com_nombre ?? "No especificada")</td>
                            </tr>
                            <tr>
                                <td class="bg-light"><strong>Fecha de Registro:</strong></td>
                                <td>@Model.FechaRegistro.ToString("dd/MM/yyyy HH:mm")</td>
                            </tr>
                        </table>
                    </div>
                </div>

              @*   <div class="alert alert-warning mt-4" role="alert">
                    <h6 class="alert-heading">
                        <i class="fas fa-info-circle"></i> Consideraciones importantes:
                    </h6>
                    <ul class="mb-0">
                        <li>Si hay información o relaciones asociadas a este recaudador, pueden perderse o quedar huérfanas</li>
                        <li>Esta acción eliminará permanentemente toda la información del recaudador</li>
                        <li>No podrás recuperar esta información una vez confirmada la eliminación</li>
                        <li>Se recomienda revisar si hay dependencias antes de eliminar</li>
                        <li>Los datos históricos relacionados podrían verse afectados</li>
                        <li>Las recaudaciones asociadas a este recaudador podrían quedar sin asignar</li>
                    </ul>
                </div> *@

                <form asp-action="Eliminar" method="post" class="mt-4">
                    @Html.AntiForgeryToken()
                    <input asp-for="Id" type="hidden" />
                    <div class="row">
                        <div class="col-12 text-center">
                            <button type="submit" class="btn btn-danger me-3" onclick="return confirm('¿Estás completamente seguro de que deseas eliminar este recaudador? Esta acción NO se puede deshacer.');">
                                <i class="fas fa-trash"></i> Sí, Eliminar Definitivamente
                            </button>
                            <a asp-action="Detalles" asp-route-id="@Model.Id" class="btn btn-info me-3">
                                <i class="fas fa-eye"></i> Ver Detalles
                            </a>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-times"></i> Cancelar
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card border-warning">
            <div class="card-header bg-warning text-dark">
                <i class="fas fa-lightbulb me-1"></i>
                Alternativas Recomendadas
            </div>
            <div class="card-body">
                <h6>En lugar de eliminar, considera:</h6>
                <ul>
                    <li><strong>Editar datos:</strong> Si sólo necesitas corregir información</li>
                    <li><strong>Cambiar compañía:</strong> Si cambió de empresa</li>
                    <li><strong>Desactivar:</strong> Si ya no está activo pero quieres mantener el historial</li>
                </ul>

                <div class="mt-3">
                    <a asp-action="Editar" asp-route-id="@Model.Id" class="btn btn-warning btn-sm w-100">
                        <i class="fas fa-edit"></i> Editar en su lugar
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>